import type { WorkspacesInfo } from "./types";
export declare class WorkspacesInfoImpl implements WorkspacesInfo {
    private sentinel;
    private root;
    private packageFilter;
    private packages;
    private findWorkspacePackages;
    private findWorkspacePackagesSync;
    constructor(sentinel: string);
    getLockfile(): string;
    getRoot(): string;
    findPackages(): Promise<string[]>;
    findPackagesSync(): string[];
    isWorkspace(packageRoot: string): boolean;
    private loadPackagesSync;
    private loadPackages;
}
//# sourceMappingURL=info.d.ts.map